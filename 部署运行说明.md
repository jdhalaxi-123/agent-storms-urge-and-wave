# stormsuregent 部署运行说明

## 一、这是什么

风暴潮与海浪智能预报「结果落地」Agent 编排层。
用户自然语言 → DeepSeek LLM → 编排引擎（两阶段）→ 文字简报 + 预报图 + 可导出 Word。

## 二、环境要求

- Windows（本交付包在 Windows 下开发验证）
- Python 3.10+（建议 3.10）
- 网络：可访问 api.deepseek.com 和腾讯云（语音识别时）

## 三、安装

```bash
# 1. 创建虚拟环境（可选）
python -m venv .venv
.venv\Scripts\activate

# 2. 安装依赖
pip install -r requirements.txt
```

## 四、配置密钥（.env）

**API 配置位置：项目根目录 `.env` 文件。**
交付包内已含模板 `.env.example`（无密钥，可直接复制）：

```
copy .env.example .env     （Windows）
# 或 cp .env.example .env  （Linux/macOS）
```

然后编辑 `.env`，填入自己的密钥：

```
DEEPSEEK_API_KEY=sk-xxxx
TENCENT_SECRET_ID=xxxx
TENCENT_SECRET_KEY=xxxx
TENCENT_ASR_REGION=ap-guangzhou
```

- `DEEPSEEK_API_KEY`：必填（对话大脑，https://platform.deepseek.com 获取）
- `TENCENT_SECRET_ID/KEY`：语音识别用；不填则回退本地 faster-whisper（首次会下载模型）
- ⚠️ `.env` 已被 .gitignore 排除，请勿提交或外发，密钥注意保密

## 五、样例数据（Sample Data）

**跑通链路用样例数据（一键生成，约 30MB）**：

```bash
python scripts/make_sample_data.py
```

生成 `data/sample/`：`output_0.nc`（数值模拟）+ `output_4.nc`（AI/融合，含驱动场）+ `ERA5/`（风场），变量结构与真实 NC 一致（elev/elevs/current/depth 等），可直接验证读取与简报链路。

**真实数据放置**（按「各课题文件路径及数据格式约定」，NC 数据放到 `data/` 目录，按台风编号分文件夹）：

```
data/
└── 2526/
    ├── output_0.nc        （数值模拟风暴潮场）
    ├── output_4.nc        （AI/融合风暴潮场）
    └── ERA5/              （再分析风场，可选）
```

- 数据自动扫描，无需配置路径
- 数据缺失时系统自动降级为骨架演示结果，链路不中断

## 六、运行

```bash
python main.py
```

浏览器打开 http://localhost:7860 ，输入示例：
> 帮我看未来5天厦门海域有没有风暴潮海水倒灌风险

## 七、目录结构

```
orchestrator/   编排层（engine/llm/asr/memory/contract/nc_inspect）
modules/        业务模块（meta/geo_stats/selector/assess/brief/brief_tpl/visualize）
scenarios/      场景路由表
main.py         Gradio 网页对话入口
```

用户自己定义的模块只需实现 `run(ctx) -> ctx`（契约见 orchestrator/contract.py）。

## 八、联系人

负责人：金智宇（总体编排）
